# TESTFLIGHT — GitHub Actions Internal (no p12)

Primary install path is `.github/workflows/testflight-internal.yml`, not Xcode Cloud.

Crisis: iPhone 12 Pro Max. Safari. No Mac. No Xcode. No p12.

## Facts

- App Blackout / CKBlackout, bundle `com.crisiskhan.blackout`, ASC `6806388963`
- Repo `crisiskhan/Blackout`
- App tree this job archives: `cursor/blackout-bible-v3-64d0` (never the old app on `main`)
- Destination: TestFlight Internal only (`28035586-fce6-474f-9bc2-ef0f1f65306e`). No App Review. No External Testing.
- This job has **no** GitHub `environment:` and must not. Auto-review / required reviewers must not gate Internal upload. If Settings → Environments later names a gate, CoS/Crisis must clear protection on that env (YAML cannot remove Settings-only rules).
- Repo CPV is 1 and collides; the job sets `CURRENT_PROJECT_VERSION` on the xcodebuild command line (max ASC + 1). It does not commit a bump.
- A 50-series TestFlight build is the old vessel.
- Signing: existing ASC AuthKey + automatic signing. No human certificate export.
- Unsigned compile stays **macos-14 + Xcode 16** (`.github/workflows/xcodebuild.yml`). TestFlight archive is **macos-15 + Xcode 26** for the iOS 26 SDK (`33931992681`). Do not use `macos-latest`. Keep App Intents processor no-ops on the selected toolchain.
- Do not `xattr -cr` the `.app` or the repo. Do not `rm`/`ditto`/`chmod 644` `Assets.car`, `PrivacyInfo.xcprivacy`, or `embedded.mobileprovision`. Do not run a chmod janitor during archive. Codesign then reports `code object is not signed at all` on the next data file.
- `STANDALONE_ICON_BEHAVIOR=none` is required: stock `33825215841` failed on loose `AppIcon60x60@2x.png`; `33825793771` with that setting did not.
- After AppIcons are gone, stock CodeSign fails on `Metadata.appintents` (`33825793771`). Keep the App Intents processor no-ops.
- After Metadata is gone, 644 `PrivacyInfo.xcprivacy` / `Assets.car` still fail as unsigned nested code (`33825608089`). Blob-sign those files. `33826265768` then failed on `embedded.mobileprovision` — blob-sign that too. Do not `xattr -cr`.
- `33827851150` (`23a7d04`): blob-sign of all three + official CodeSign of `Blackout.app` succeeded. `xcodebuild archive` then failed `Archive Missing Bundle Identifier` (exit 70) and tore down `InstallationBuildProductsLocation`. `BuildProductsPath/Release-iphoneos/Blackout.app` is only a symlink to that same path, so post-archive recover found nothing.
- FACT (33827851150): `ProcessInfoPlistFile` ran `builtin-infoPlistUtility` on `Blackout/Info.plist` + `assetcatalog_generated_info.plist` only. Source plist was `NSBonjourServices` only. `PRODUCT_BUNDLE_IDENTIFIER=com.crisiskhan.blackout` and `GENERATE_INFOPLIST_FILE=YES` were in the env. CodeSign + Validate + Touch succeeded; IDE archive packaging then exited 70.
- INFERENCE revised by `33829001016`: the processed iOS `.app` **had** `CFBundleIdentifier=com.crisiskhan.blackout`. Archive packaging still failed to write the xcarchive-root `Info.plist` `ApplicationProperties` (Products/dSYMs/Signatures present, archive Info.plist absent). The missing-identifier error is the IDE archive wrapper, not an empty iOS app plist.
- FACT (`33829001016` `ProcessInfoPlistFile`): `GENERATE_INFOPLIST_FILE=YES` did **not** pass a generated Info.plist into `builtin-infoPlistUtility`. Watch (`product-type.application`) and Widgets were processed from their source plists only. Watch source had `WKWatchOnly` + companion id — **no `CFBundleIdentifier`**. Widget source had NSExtension only — **no `CFBundleIdentifier`**. Main app `PRODUCT_BUNDLE_IDENTIFIER=com.crisiskhan.blackout` / `SKIP_INSTALL=NO` were already in the env.
- Project fix: every archived product Info.plist now carries `CFBundleIdentifier=$(PRODUCT_BUNDLE_IDENTIFIER)` plus the packaging keys ProcessInfoPlistFile actually copies (executable / package type / versions). Main target sets `SKIP_INSTALL=NO` and `INSTALL_PATH=$(LOCAL_APPS_DIR)` explicitly. Prefer stock `xcodebuild archive` + `exportArchive`. Do not delete `PrivacyInfo` / `Assets.car` / AppIcons.
- `33829001016` (`43f13b9`): processed iOS `CFBundleIdentifier=com.crisiskhan.blackout` / version 54. Snapshot ran. Archive packaging still exited 70, but left `Blackout.xcarchive/Products/Applications/Blackout.app` + dSYMs + Signatures and **no** xcarchive `Info.plist`. Recover overwrote the snapshot with that already-signed product, `rm -rf _CodeSignature`, then `codesign` reported `bundle format unrecognized`. Do not re-seal a signed archive product. CI may still write a missing xcarchive `Info.plist` and `exportArchive` or hand-zip if packaging fails; the project fix is Watch/Widget identifiers so stock archive can write ApplicationProperties.
- `33907781589`: archive + IPA succeeded (inject CFBundleVersion 54). `altool` upload failed −19000 — no ASC application record for `com.crisiskhan.blackout.watchkitapp`. Crisis cut: **Watch is omitted from the App Store / TestFlight archive** so phone Internal can land. Widget stays embedded. The `BlackoutWatch` target and `BlackoutWatch.xcscheme` remain in the project for later.
- `33925258357` (`4456206`): KEEP Dist cert reuse + Watch omitted. Hand-zip IPA ready. `altool` −19000 on **`com.maplibre.mapbox`** (MapLibre.framework Info.plist). Do not create an ASC app for MapLibre.
- `33929367958` (`0e3d31d`): signing + archive + IPA succeeded (Local Dist `FG5MXH5347`, CPV 54). `altool` −19000 on **`com.crisiskhan.blackout.maplibre`**. Upload had no `--apple-id` / `--bundle-id`, so altool picked the nested FMWK BID. Bind upload to ASC `6806388963` + `com.crisiskhan.blackout`. Do **not** rewrite nested FMWK onto `com.crisiskhan.blackout.*`. Strip FMWK `CFBundleIdentifier` (keep `CFBundlePackageType=FMWK`) or leave a foreign vendor id. Still do not create an ASC app for MapLibre.
- Archive exit 70 `Archive Missing Bundle Identifier` after the main app plist already had `com.crisiskhan.blackout`: the extra archived product was the **Vendor/MapLibre xcframework** linked beside `MapLibreMap`. That XFWK wrapper Info.plist has `CFBundlePackageType=XFWK` and **no** `CFBundleIdentifier`. App/widget source plists already carry `CFBundleIdentifier=$(PRODUCT_BUNDLE_IDENTIFIER)`. Watch stays omitted from the App Store archive.
- App target now links **MapLibreMap only** (MapLibreMap already depends on the vendor binary). `tf-archive.sh` runs `tools/tf_ipa_inspect.py` after the IPA exists: assert app/widget BIDs, require nested FMWK vendor BIDs `com.maplibre.mapbox` (MapLibre) and `org.ggml.llama` (llama) (do not strip; do not rewrite onto owned), fail closed on foreign `Payload/*.app` / `PlugIns/*.appex`. Upload uses `xcrun altool --upload-package` with `--apple-id "$ASC_APP_ID"` `--bundle-id com.crisiskhan.blackout` `--bundle-version` `--bundle-short-version-string`.
- `33926435868` (`e096955`): KEEP reuse, `HAS_LOCAL_DIST_KEY=0`, no Manual patch. `xcodebuild archive` then asked for **Apple Development** certs/profiles (`Revoke certificate` / no iOS App Development profiles) and exited 65 — no IPA. Fix: even when KEEP Dist `45YLWHL6UP` exists, mint a **runner-local Dist cert** (`HAS_LOCAL_DIST_KEY=1`) and bind Local-named App Store profiles (`Blackout iOS App Store GHA Local` / `Blackout Widgets App Store GHA Local`) to that cert. Leave KEEP-named ACTIVE profiles alone. Never revoke KEEP. If Dist create hits Apple’s cap, fail closed (do not auto-revoke KEEP). Optional: revoke only `IOS_DEVELOPMENT` / `DEVELOPMENT` certs named `Created via API` that block AuthKey.
- `33927056130` (`ce331dd`): mint worked (`HAS_LOCAL_DIST_KEY=1`). Archive still exit 65 because CLI `CODE_SIGN_IDENTITY="iPhone Distribution"` applied to **every** target, including SPM packages (`VisionCoreML`, `Tokens`, `MapLibreMap`) that stay Automatic: “automatically signed for development, but a conflicting code signing identity iPhone Distribution has been manually specified.” Do **not** pass `CODE_SIGN_IDENTITY` on the `xcodebuild archive` or `exportArchive` command line. Keep the per-target CI pbx patch (Manual + Dist identity + Local profile) on app/widget only. The patch and keychain-hash rewrite share one placeholder (`iPhone Distribution`) so the rewrite cannot miss (`iOS Distribution` / `Apple Distribution` aliases also rewrite).
- `33928044175` (`6e1e411`): mint created Dist `YVK8HM9GT2` (IOS_DISTRIBUTION was HTTP 409 — Apple already had `2LWNR93SGQ` + KEEP `45YLWHL6UP`). Archive never ran: ACTIVE Local profile `Blackout iOS App Store GHA Local` was still bound to the previous mint. Each runner is ephemeral, so the next flight must (1) revoke stale **non-KEEP** Dist leftovers so the cap has a slot, (2) replace Local-named profiles onto the new local Dist, including INVALID leftovers after that Dist revoke. Never revoke KEEP. Never delete KEEP-named profiles (`Blackout iOS App Store GHA` / `Blackout Widgets App Store GHA`).
- `33930228429` (`31fcfe5`): Dist prune + iOS Local replace succeeded (`KR7N96LT42`). Widget Local CREATE then ASC HTTP 500 `UNEXPECTED_ERROR`. Dist revoke leaves the previous Local profile `INVALID`; an ACTIVE-only list misses it so CREATE collides. List `ACTIVE,INVALID`. Retry Local-named CREATE/replace-recreate on 500/UNEXPECTED_ERROR (5 attempts, backoff 15s/30s/60s/90s/120s, re-list between attempts). Cool down ~25s after an iOS Local write before Widgets Local create/replace. Never delete KEEP-named profiles.
- `33931034850` (`f4ba3af`): signing + archive + hand-zip IPA succeeded (Local Dist `FQK7KQDXGP`, CPV 54, inspect no FMWK rewrite). altool bound the primary app, then 409: `Payload/Blackout.app` is not signed using an Apple submission certificate. ExportOptions asked for Apple Distribution while the keychain identity was `iPhone Distribution`; exportArchive looked for `3rd Party Mac Developer Installer` (`Bundle identifier is missing`). Write ExportOptions `signingCertificate` from `security find-identity` (iPhone / Apple / iOS Distribution). Fill xcarchive ApplicationProperties BID/versions/Team before exportArchive (`app-store`, no Mac installer). Hand-zip re-signs nested code if Authority is not a submission Dist identity, then fail-closed — do not hand altool a rejected IPA.
- `33931992681` (`d86628c`): Apple Distribution mint `VN8S62458Z`, ExportOptions `Apple Distribution`, archive product already had submission Authority, ditto hand-zip, `codesign --verify` OK. altool bound the primary app (no −19000). Then 409: reserved `Blackout.app/Resources` (`ditto --sequesterRsrc` **and** the Xcode copy of repo `Resources/` into the `.app`); MapLibre `CFBundleIdentifier ''` / Identifier `MapLibre` vs `$bundleIdentifier`; iOS 18.2 SDK not accepted (need iOS 26 SDK / Xcode 26). Keep vendor `com.maplibre.mapbox` (not an owned ASC invent). Hand-zip `ditto --norsrc`. Flatten repo Packs/Field/Vision into the `.app` root (no reserved `Resources/` directory); IPA inspect + hand-zip fail-closed if it remains, then re-sign. codesign `--identifier` must equal app/widget/FMWK BID. TF runner **macos-15 + Xcode 26**; unsigned compile stays **macos-14 + Xcode 16**. CoS must copy the yml to `main` (runs-on lives there). No MapLibre ASC app. KEEP Dist `45YLWHL6UP` untouched.
- `33986112949` (`3ae500a`): archive + altool OK, CPV **56** `VALID`, PATCH 200, then `ASSIGN Internal 56 404` (`NOT_FOUND` on that build id). Build is on ASC; Internal group attach raced. Retry betaGroups 404 (15s/30s/60s/90s/120s). 409 stays success (already assigned). CoS must copy the yml so the assign step calls `tools/tf_asc_assign.py`.
- `33987452079` (`7095fa1`): macos-15 / Xcode 26.3. `exportArchive OK`. CPV **57**. Dist `R8P2DS495P` (`DISTRIBUTION`). altool no errors. ASC `96af7288-860d-4a28-8009-b4a1ed8d47b7` **VALID**. PATCH 200. `ASSIGN Internal 57 204`. Tip-57 streets + pack bbox + YOU. Later tip-58 (`88b1726`) is not in this IPA.
- `34002236273` (`67f9b5f`): macos-15 / Xcode 26.3. `exportArchive OK`. CPV **58**. Dist `AN8MADVN3Z` (`DISTRIBUTION`). altool no errors. ASC `b4063ed9-f737-4da1-81bf-1206f625e342` **VALID**. PATCH 200. `ASSIGN Internal 58 204`. Tip-58 MARK/timer/RED/PTT plus tip-57 streets.
- `34002862630` (`919dfa8`): first `tf:` push auto-upload. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **59**. Dist `6L4U8L695G` (`DISTRIBUTION`). altool no errors. ASC `3da30d72-408b-4cd2-a6e7-7009106e97bb` **VALID**. PATCH 200. `ASSIGN Internal 59 204`. Same tip-58 app as 58 (docs-only tree).
- `34232353416` (`b451bb1`): tip-60 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **60**. Dist `U8C9ZLHLTJ` (`DISTRIBUTION`). altool no errors. ASC `4bcbdb70-f766-4d5d-8aff-8315399e56ca` **VALID**. PATCH 200. `ASSIGN Internal 60 204`. Leave 59 until TestFlight shows 60 Ready.
- `34237111681` (`b947536`): tip-61 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **61**. Dist `422W3DA782` (`DISTRIBUTION`). altool no errors. ASC `b7c98209-6c8d-4f42-a209-cbff4c63c789` **VALID**. PATCH 200. `ASSIGN Internal 61 204`. TX WEST walkable OSM. Leave 60 until TestFlight shows 61 Ready.
- `34245270215` (`b0601e2`): tip-62 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **62**. Dist `RMG3W7ARUD` (`DISTRIBUTION`). altool no errors. ASC `198debb6-312a-41ba-b1a7-c4ff6d683456` **VALID**. PATCH 200. `ASSIGN Internal 62 204`. MAP chips + WALK/DRIVE. Leave 61 until TestFlight shows 62 Ready.
- `34248224189` (`35ad515`): tip-63 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **63**. Dist `7CTCLZKX2Z` (`DISTRIBUTION`). altool no errors. ASC `53323e29-bd51-426e-88bf-bf0fdb557b90` **VALID**. PATCH 200. `ASSIGN Internal 63 204`. NM walkable pack; tx-west default. Leave 62 until TestFlight shows 63 Ready.
- `34257680002` (`9eb32c4`): tip-64 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **64**. Dist `S3R65K88HJ` (`DISTRIBUTION`). altool no errors. ASC `5116f1c0-09a3-4419-a304-d6ea86914856` **VALID**. PATCH 200. `ASSIGN Internal 64 204`. TX WEST arterial casing + highway refs. Leave 63 until TestFlight shows 64 Ready.
- `34260710700` (`f401a3d`): tip-65 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **65**. Dist `2T2U9RQMJD` (`DISTRIBUTION`). altool no errors. ASC `94bcaf54-c944-47db-a399-b373a7be2012` **VALID**. PATCH 200. `ASSIGN Internal 65 204`. Speak turn-by-turn + TX EAST. Leave 64 until TestFlight shows 65 Ready.
- `34261557379` (`c55d861`): tip-66 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **66**. Dist `33BKW9XRQN` (`DISTRIBUTION`). altool no errors. ASC `b3310736-f1d6-4ea8-96a4-c40eccb9e770` **VALID**. PATCH 200. `ASSIGN Internal 66 204`. Same Speak + TX EAST tree as 65. Leave 65 until TestFlight shows 66 Ready.
- `34264110982` (`960a8a2`): tip-67 `tf:` push. Mint OK (`HAS_LOCAL_DIST_KEY=1`), then GET `/v1/builds` HTTP 500 in Next CPV. No IPA. Retry list 500 (15s/30s/60s/90s/120s) via `tools/tf_asc_cpv.py`. Do not re-upload 54–66.
- `34264462418` (`960a8a2`): tip-67 `workflow_dispatch` with a concatenated `git_ref`. Checkout fetch failed. No IPA.
- `34264699162` (`960a8a2`): tip-67 `workflow_dispatch` retry. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **67**. Dist `Q8AX4T282L` (`DISTRIBUTION`). altool no errors. ASC `e2cf188e-1fa8-4e07-b306-1e088087c7d0` **VALID**. PATCH 200. `ASSIGN Internal 67 204`. GraphProbe + warmup + keep-awake. Leave 66 until TestFlight shows 67 Ready.
- `34269174169` (`20d2df7`): tip-68 `tf:` push. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **68**. Dist `S4B68PPMFK` (`DISTRIBUTION`). altool no errors. ASC `6f4ebfca-b491-408e-831a-e63a1bac63cb` **VALID**. PATCH 200. `ASSIGN Internal 68 204`. Speak finish + clean field + walking-zoom names. Leave 67 until TestFlight shows 68 Ready.
- `34463051408` (`9911aaf`): HUD tag `tf-79`. CPV **79**. Unnamed tank sure. Leave 78 until TestFlight showed 79 Ready.
- `34464769463` (`15516ec`): HUD tag `tf-80`. CPV **80**. Compass mark as App Icon and boot logo. Leave 79 until TestFlight shows 80 Ready.
- `34480452541` (`98bb68a`): HUD tag `tf-81`. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **81**. Dist `TPTQ6GKWZ7` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool no errors. `WAIT no build 81`, then ASC `b4d4d85f-7f42-4338-a38c-58e0c3ae4591` **VALID**. PATCH 200. `ASSIGN Internal 81 204`. HUD on every tab + hold water classify + named marks. Leave 80 until TestFlight shows 81 Ready.
- `34491969097` (`323aa7f`): HUD tag `tf-82`. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **82**. Dist `8872S8A8NY` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool no errors. `WAIT no build 82`, then ASC `f5262825-9fc6-44a3-90c5-4eade1cee20b` **VALID**. PATCH 200. `ASSIGN Internal 82 204`. Textless compass + HUD mark language through the instrument. Leave 81 until TestFlight shows 82 Ready.
- `34502826993` (`3abaf58`): HUD tag `tf-83`. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **83**. Dist `947QX8HSZ9` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool no errors. `WAIT no build 83`, then ASC `9ef5ecfb-f11a-40a8-b2c9-27148f0a02ea` **VALID**. PATCH 200. `ASSIGN Internal 83 204`. Expedition HUD + FIELD vision still + COMMS call. Leave 82 until TestFlight shows 83 Ready.
- `34536953430` (`4ec42d4`): HUD tag `tf-84`. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **84**. Dist `WK9VCHNP39` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool no errors. `WAIT no build 84`, then ASC `e0ac0030-87e3-4952-b2a7-8336dffe99ef` **VALID**. PATCH 200. `ASSIGN Internal 84 204`. Scarce red, no fake party bodies, chrome that actually sleeps. Leave 83 until TestFlight shows 84 Ready.
- `34547063820` (`487f290`): HUD tag `tf-85`. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **85**. Dist `JVJ9WKTV5A` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool no errors. `WAIT no build 85`, then ASC `5cbd4db3-9642-4892-bef4-e8542a75afa9` **VALID**. PATCH 200. `ASSIGN Internal 85 204`. Hold ground + VISION still open this pack's plant, bite, animal, and cave cards. Never edible. Leave 84 until TestFlight shows 85 Ready.
- `34550033439` (`0d0e0b3`): HUD tag `tf-86`. macos-15 / Xcode 26.3. `exportArchive OK`. CPV **86**. Dist `6KKC39285N` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 86`, then ASC `f1a73104-c56c-4171-ba94-c08b0853c716` **VALID**. PATCH 200. `ASSIGN Internal 86 204`. Tree-use first on woodland, `BOOK` + `CARD n OF`, named tree is not woodland, hole walks cave then cold. Never edible. Leave 85 until TestFlight shows 86 Ready.
- `34572153310` (`2c91036`): HUD tag `tf-87`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **87**. Dist `SJ7LCMGH3L` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 87`, then ASC `6ab6b596-0c4f-4a60-89f6-6cc39dfad22b` **VALID**. PATCH 200. `ASSIGN Internal 87 204`. `usesNonExemptEncryption: null`. Peak walk includes bite; NM mammal SPEAK names the bite card. Never edible. Leave 86 until TestFlight shows 87 Ready.
- `34629533409` (`6295e73`): HUD tag `tf-88`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **88**. Dist `4JYX5MF425` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 88`, then ASC `a0a32168-94a7-422d-8204-31332e32eaa1` **VALID**. PATCH 200. `ASSIGN Internal 88 204`. `usesNonExemptEncryption: null`. Notable OSM reserves, caves, and named trees; karst / wildlife preserve / Audubon / nature area / habitat preserve Holds. Never edible. Leave 87 until TestFlight shows 88 Ready.
- `34648083111` (`c1388f3`): HUD tag `tf-89`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **89**. Dist `NVH99KH233` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 89`, then ASC `24e2c615-db24-4f09-b362-563cdafd1d4c` **VALID**. PATCH 200. `ASSIGN Internal 89 204`. `usesNonExemptEncryption: null`. FIELD SEARCH, from-nothing book, botanic Holds (Lush n Lean / Orchard Garden / Harvey Cornell). Never edible. Leave 88 until TestFlight shows 89 Ready.
- `34745805359` (`adfb764`): HUD tag `tf-99`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **99**. Dist `66KB44VS8G` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 99`, then ASC `960c7bd3-fe7e-47a2-a5dc-55c53ae9db55` **VALID**. PATCH 200. `ASSIGN Internal 99 204`. `usesNonExemptEncryption: null`. Live instruments, SPEAK street vertices, unique leftover Holds. Historical — scoring tip is **117**.
- `34745993995` (`cfd2668`): HUD tag `tf-100`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. Dist `F5Z894XRY6` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Next CPV raced tf-99 and minted **99** again. altool `-19232` duplicate bundle version 99. No unique IPA. Assign reused ASC `960c7bd3-fe7e-47a2-a5dc-55c53ae9db55` **VALID** and `ASSIGN Internal 99 204`. COORDINATES word off the MAP box is not on TestFlight as its own build. CPV **100** is tag `tf-101`.
- `34752403614` (`9c33aa8`): HUD tag `tf-115`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **108**. Dist `8CWLXNF8C7` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. altool uploaded CFBundleVersion 108. `WAIT no build 108`, then ASC `0315a666-fda5-4fed-924e-fdd1f47cdc2b` **VALID**. PATCH 200. `ASSIGN Internal 108 204`. `usesNonExemptEncryption: null`. Tag 115 is not TestFlight 115 — tags 101–109 minted CPV 100–107. Historical — scoring tip is **117**.
- `34759178121` (`93a1b5d`): HUD tag `tf-116`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **109**. Dist `2UCJZVQLL3` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `8CWLXNF8C7` revoked (not KEEP). altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 109`, then ASC `929c7c0e-0be8-4676-bc73-030f697ee76b` **VALID**. PATCH 200. `ASSIGN Internal 109 204`. `usesNonExemptEncryption: null`. Tag 116 is not TestFlight 116. Historical — scoring tip is **117**.
- `34763432123` (`7fbaf83`): HUD tag `tf-117`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **110**. Dist `DNYUTN8JX3` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `2UCJZVQLL3` revoked (not KEEP). altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 110`, then ASC `f598d2c6-808b-45e5-be64-7283349b0ca0` **VALID**. PATCH 200. `ASSIGN Internal 110 204`. `usesNonExemptEncryption: null`. Tag 117 is not TestFlight 117. Scoring IPA is `0.1.0 (110)`. Leave 109 until TestFlight shows 110 Ready.
- `35230916810` (`a227799`): CCTV tag `tf-176`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **149**. Dist `D444F3KQB7` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. IPA **4207460012** bytes. altool `UPLOAD SUCCEEDED with no errors`. Then ASC `1463e5f8-6028-4620-8bb8-8a9b5b8593e8` **INVALID**. Same ceiling as tf-174 CPV 148 (4212485648). No Internal assign. Do not retag 176.
- `35236202236` (`545b7b2`): CCTV tag `tf-177`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **150**. Dist `5684ZXQZ73` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `D444F3KQB7` revoked (not KEEP). IPA **3469009137** bytes (under 4e9). altool `UPLOAD SUCCEEDED with no errors`. ASC `76e89783-1568-40f0-95f9-4dd9fa858190` **VALID**. PATCH 200. `ASSIGN Internal 150 204`. `usesNonExemptEncryption: null`. NM NAIP left off the phone copy. Packed CCTV + SNAP stills. Historical — scoring tip is **151**.
- `35275834080` (`304a914`): CCTV tag `tf-178`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **151**. Dist `3FRJL249S4` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `5684ZXQZ73` revoked (not KEEP). IPA **3469008911** bytes. altool `UPLOAD SUCCEEDED with no errors`. ASC `6e68c343-a27c-4391-b785-1991017d7bae` **VALID**. PATCH 200. `ASSIGN Internal 151 204`. `usesNonExemptEncryption: null`. Phone style strips missing NAIP; PackStyle resolver v14. Historical — scoring tip is **154**.
- `35290763245` (`ec3b586`): FIELD tag `tf-179`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **152**. Dist `GAK7G43DQA` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `3FRJL249S4` revoked (not KEEP). IPA **3469219881** bytes. altool `UPLOAD SUCCEEDED with no errors`. ASC `22d8dbc9-f996-4c62-8145-1236549cf7e3` **VALID**. PATCH 200. `ASSIGN Internal 152 204`. `usesNonExemptEncryption: null`. Field-medic tree. Historical — scoring tip is **154**.
- `35293346292` (`1cc44f5`): mesh-near tag `tf-180`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **153**. Dist `9FQZG9MZ95` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `GAK7G43DQA` revoked (not KEEP). IPA **3469309555** bytes. altool `UPLOAD SUCCEEDED with no errors`. ASC `ed3eda8a-d8b8-4e2a-8f54-154fdf3a0481` **VALID**. PATCH 200. `ASSIGN Internal 153 204`. `usesNonExemptEncryption: null`. NEAR dots + infant belly. Historical — scoring tip is **154**.
- `35299262226` (`32a0ad2`): photo-desk tag `tf-181`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **154**. Dist `2SK8A5QCM9` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `9FQZG9MZ95` revoked (not KEEP). IPA **3469334742** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 154`, then ASC `bea37b7d-ffd7-451a-a7d1-95d999542c74` **VALID**. PATCH 200. `ASSIGN Internal 154 204`. `usesNonExemptEncryption: null`. No grey house masses. Historical — scoring tip is **155**.
- `35303551868` (`b32b344`): photo-desk tag `tf-182`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **155**. Dist `5RHV6UMX77` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `2SK8A5QCM9` revoked (not KEEP). IPA **3469335131** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 155`, then ASC `d5e72674-de74-4acb-8633-1c806739537f` **VALID**. PATCH 200. `ASSIGN Internal 155 204`. `usesNonExemptEncryption: null`. Dest pins above first WALK casing. Historical — scoring tip is **156**.
- `35340721914` (`e3ef714`): mesh-scan tag `tf-183`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **156**. Dist `6WGC4GGVHS` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `5RHV6UMX77` revoked (not KEEP). IPA **3469344272** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 156`, then ASC `806c9402-19dc-4b67-8dbf-25a22fdf82f8` **VALID**. PATCH 200. `ASSIGN Internal 156 204`. `usesNonExemptEncryption: null`. SCAN/JOIN place no-fix hears around YOU. Historical — scoring tip is **157**.
- `35357713203` (`66d6e0c`): condition-rail tag `tf-184`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **157**. Dist `536BH2LGA9` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `6WGC4GGVHS` revoked (not KEEP). IPA **3469430803** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 157`, then ASC `f0dfe7aa-8d7e-430c-8198-f3ef50ebee77` **VALID**. PATCH 200. `ASSIGN Internal 157 204`. `usesNonExemptEncryption: null`. Glass capsule CONDITION rails + diary under ROSTER. Historical — scoring tip is **158**.
- `35372832850` (`b41b8c7`): mesh-hear tag `tf-185`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **158**. Dist `P3SZNNKP7D` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `536BH2LGA9` revoked (not KEEP). IPA **3469430781** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 158`, then ASC `c2431345-9098-463d-b106-34d82fee8af8` **VALID**. PATCH 200. `ASSIGN Internal 158 204`. `usesNonExemptEncryption: null`. MeshHear test labels + capsules + diary. Historical — scoring tip is **159**.
- `35448323976` (`168c7f6`): photo-seam tag `tf-186`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **159**. Dist `9Z74QKR88Y` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `P3SZNNKP7D` revoked (not KEEP). IPA **3493500636** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 159`, then ASC `f81c6e01-0072-47b3-aa9e-04f6747e7ff9` **VALID**. PATCH 200. `ASSIGN Internal 159 204`. `usesNonExemptEncryption: null`. STATES overview + TX WEST walk-desk feather. Historical — scoring tip is **160**.
- `35463190384` (`06d98b6`): drop-khan-eye tag `tf-187`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **160**. Dist `D48KPGX9LT` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `9Z74QKR88Y` revoked (not KEEP). IPA **3493435231** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 160`, then ASC `33b38bb8-109c-4d4b-b312-1408abdc3db2` **VALID**. PATCH 200. `ASSIGN Internal 160 204`. `usesNonExemptEncryption: null`. KHAN EYE gone; walking desk pinch 17.5. Historical — scoring tip is **161**.
- `35582125243` (`aef6a73`): dead-glass tag `tf-188`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **161**. Dist `SBV25QJ9C6` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `D48KPGX9LT` revoked (not KEEP). IPA **3493501831** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 161`, then ASC `92522e5e-6caf-48ae-afd5-88bf30032123` **VALID**. PATCH 200. `ASSIGN Internal 161 204`. `usesNonExemptEncryption: null`. POWER POCKET only; CLIP PLAY; paper share. Historical — scoring tip is **162**.
- `35597277741` (`e41bcb9`): map-crash tag `tf-189`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **162**. Dist `S986PFDJ7G` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `SBV25QJ9C6` revoked (not KEEP). IPA **3493485365** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 162`, then ASC `00b3bb36-31e4-4670-b281-055687cde986` **VALID**. PATCH 200. `ASSIGN Internal 162 204`. `usesNonExemptEncryption: null`. PACK-switch style skip; three yellow is ORANGE; FACE/PAPER off glass; wired HOLD PTT. Historical — scoring tip is **163**.
- `35627814042` (`b28c349`): desk-hold tag `tf-190`. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **163**. Dist `KJP9DGH63Z` (`DISTRIBUTION`). KEEP `45YLWHL6UP` untouched. Stale runner Dist `S986PFDJ7G` revoked (not KEEP). IPA **3493523793** bytes. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 163`, then ASC `463fd5c8-8a5a-4d39-ab91-00ce589fdb72` **VALID**. PATCH 200. `ASSIGN Internal 163 204`. `usesNonExemptEncryption: null`. Desk holds YOU / DEST / body / kit / timers / FIELD card / ES/EN; silent WALK replan. Scoring IPA is `0.1.0 (163)`. Leave 162 until TestFlight shows 163 Ready.

## Watch omitted from App Store archive (re-enable later)

Scheme **Blackout** (what GHA archives) does **not** embed `BlackoutWatch.app`. There is no Embed Watch Content phase and no Blackout → BlackoutWatch target dependency. The exported IPA must not contain `Payload/Blackout.app/Watch/` or a `.watchkitapp`. `tf-archive.sh` fails closed if it does.

Do **not** create an ASC Watch companion app just to unblock phone Internal.

To put Watch back in the uploaded iOS IPA (only after ASC has a companion record for `com.crisiskhan.blackout.watchkitapp`):

1. In `Blackout.xcodeproj/project.pbxproj` (and `tools/v3/generate_project.py` so regen matches): restore **Embed Watch Content** (`dstPath = $(CONTENTS_FOLDER_PATH)/Watch`, copy `BlackoutWatch.app`) on the Blackout target, and restore the Blackout → BlackoutWatch `PBXTargetDependency`.
2. Add `com.crisiskhan.blackout.watchkitapp` back to `BUNDLES` in `tools/tf_asc_reuse.py` (platform `WATCHOS`, not `IOS`) and to the signing `spec` in `.github/ci/tf-archive.sh`.
3. Keep Watch `CFBundleIdentifier=$(PRODUCT_BUNDLE_IDENTIFIER)` in `BlackoutWatch/Info.plist`.
4. Confirm the IPA listing includes `Payload/Blackout.app/Watch/BlackoutWatch.app` before upload. Then remove the “no Watch/ companion” fail-closed check in `tf-archive.sh`, or invert it to require Watch.
5. Do not bump tree `CURRENT_PROJECT_VERSION`. Do not add a strip script.

Local Watch compile: scheme **BlackoutWatch**, destination `generic/platform=watchOS`. Unsigned `xcodebuild.yml` still builds scheme **Blackout** for iOS only (Widget embedded; Watch not).

GitHub requires a workflow file on the default branch for the Actions **Run workflow** button. **Do not use that button against `main` until CoS syncs host YAML.** Main still has watchkitapp in `BUNDLES`; tip does not. Tip YAML is the source of truth.

## How to fire Internal (bible-v3 only)

Push triggers live on tip YAML (`cursor/blackout-bible-v3-64d0`). Do not add `main` as a trigger. Do not merge this job to main to “enable” it — a push to bible-v3 uses the workflow file on that branch.

1. **Commit message** on `cursor/blackout-bible-v3-64d0` starting with `tf:` (example: `tf: streets + tip-58`). Any other prefix (including `ci(tf):`) does **not** upload.
2. **Tag** matching `tf-*` (example: `git tag tf-58 && git push origin tf-58`). The tagged commit must contain this workflow.
3. **Backup** `workflow_dispatch` with `git_ref` (same as before):

```bash
gh workflow run "TestFlight Internal" --ref cursor/blackout-bible-v3-64d0 -f git_ref=cursor/blackout-bible-v3-64d0
```

`--ref` selects the workflow file. `-f git_ref=` selects the app tree to archive. Both must be the tip. Do not dispatch `--ref main`. Do not bump tree CPV. Agents must not dispatch a TestFlight upload unless asked to verify.

A push to bible-v3 that does **not** start with `tf:` still starts the workflow file, then **skips** the upload job. That skip is intentional so CI YAML edits do not burn ASC quota.

First automated Internal upload: this `tf:` push on bible-v3. Tree CPV stays 1. Watch omitted. Internal only.

GHA `33924134240` / `33924251037`: tip still **deleted** stale GHA App Store profiles then POSTed new ones and ASC returned HTTP 500 `UNEXPECTED_ERROR`. Archive never ran; CPV 54 was not minted. Keep Dist cert `45YLWHL6UP` as reference. Always mint a runner-local Dist cert for the archive. Reuse or create Local-named profiles (`Blackout iOS App Store GHA Local` / `Blackout Widgets App Store GHA Local`) bound to that local cert. Do not delete KEEP-named `Blackout iOS App Store GHA` / `Blackout Widgets App Store GHA`. Never revoke KEEP.

## How Crisis runs it from iPhone Safari

Preferred: GitHub → this repo → branch `cursor/blackout-bible-v3-64d0` → edit or commit with message starting `tf:` (or create tag `tf-58` on that tip). That push fires Internal. Do not use a `tf:` prefix on commits that must not upload.

Safari **Run workflow** lists the default-branch YAML and stays the backup. Until CoS copies **only** this tip yml onto `main` (app tree stays unmerged), the button cannot pick tip YAML. CoS can still run the `gh workflow run --ref` command above.

After a green job: TestFlight app → Blackout → new build (not 50-series) → Install. Airplane On, BT On. Score `docs/SOLO_QA.md`.

## Mac-optional / do not use

Do not export a p12. Unsigned `.github/workflows/xcodebuild.yml` does not read ASC secrets and must stay green without them.

## Tip 60 Internal (CoS 2026-09-08)

SOURCE GO tip `e4e2e7e91ca3879a22fca1295dea48cbfb2d0618` (product `47a9edfc`). Five chrome PASS. Run `34232353416` uploaded CPV **60** and `ASSIGN Internal 60 204`. Leave 59 until TestFlight shows 60 Ready. Watch omitted. Internal only. Do not re-upload 54–59.

## Tip 61 Internal (CoS 2026-09-08)

SOURCE GO tip `9ae26f18ee5eb15a6f529b80af4e801c5271a309` BEST IN CLASS TX WEST walkable OSM (~48.3 MB; streets at walking zoom yes). Run `34237111681` uploaded CPV **61** and `ASSIGN Internal 61 204`. Leave 60 until TestFlight shows 61 Ready. Watch omitted. Internal only. Do not re-upload 54–60.

## Tip 62 product (no Internal yet)

WALK/DRIVE on-graph polyline on the existing TX WEST pack. Graph already shipped in tip 61 (`Resources/Packs/tx-west/graph.json`, ~264k edges). Do not `tf:` until CoS+Crisis GO. Tree CPV stays 1. Watch omitted.

## Tip 62 Internal (CoS 2026-09-08 Crisis GO)

SOURCE GO tip `81a8903a82bf9a6f717e960793da72e5b508b39f` (tree also has `a410b47` 44pt chips). Factory mode. Run `34245270215` uploaded CPV **62** and `ASSIGN Internal 62 204`. Leave 61 until TestFlight shows 62 Ready. Watch omitted. Internal only. Do not re-upload 54–61.

## Tip 63 Internal (CoS 2026-09-08 Director factory)

NM walkable pack tip `52cac90b` merge `42b61974` (~64.1 MB streets yes). tx-west stays default. Run `34248224189` uploaded CPV **63** and `ASSIGN Internal 63 204`. Leave 62 until TestFlight shows 63 Ready. Watch omitted. Internal only. Do not re-upload 54–62.

## Tip 64 Internal (CoS 2026-09-08)

SOURCE GO tip `556b4a5b` / merge `999e110a`. Run `34257680002` uploaded CPV **64** and `ASSIGN Internal 64 204`. TX WEST arterial red casing + readable highway refs. Leave 63 until TestFlight shows 64 Ready. Watch omitted. Internal only. Do not re-upload 54–63.

## Tip 65 Internal (CoS 2026-09-08)

SOURCE GO tip `344094f4` / merge `955c3b24`. Run `34260710700` uploaded CPV **65** and `ASSIGN Internal 65 204`. Speak turn-by-turn + names + TX EAST pack. Leave 64 until TestFlight shows 65 Ready. Watch omitted. Internal only. Do not re-upload 54–64.

## Tip 66 Internal (CoS 2026-09-08)

SOURCE GO tip `09e4fffe` / merge `1eaa33b5`. Run `34261557379` uploaded CPV **66** and `ASSIGN Internal 66 204`. TX EAST ~65.6 MB; default tx-west. Same Speak tree as 65. Leave 65 until TestFlight shows 66 Ready. Watch omitted. Internal only. Do not re-upload 54–65.

## Tip 67 Internal (CoS 2026-09-08)

SOURCE GO tip `b7953348` / merge `a72a8478`. Run `34264699162` uploaded CPV **67** and `ASSIGN Internal 67 204`. Map keep-awake + graph warmup. Leave 66 until TestFlight shows 67 Ready. Watch omitted. Internal only. Do not re-upload 54–66.

## Tip 68 Internal (CoS 2026-09-08)

SOURCE GO tip `afb2e2fc` / merge `7abb9e0b`. Run `34269174169` uploaded CPV **68** and `ASSIGN Internal 68 204`. Speak banner finish + clean field + walking-zoom names. Leave 67 until TestFlight shows 68 Ready. Watch omitted. Internal only. Do not re-upload 54–67.

## HUD 79–80 Internal

HUD side path, not bible-v3. Tag `tf-79` (`9911aaf`, run `34463051408`) uploaded CPV **79**. Tag `tf-80` (`15516ec`, run `34464769463`) uploaded CPV **80** — compass mark as App Icon and boot logo. Watch omitted. Internal only. Do not re-upload 54–80.

## HUD 81 Internal (Crisis GO)

HUD tree `98bb68a` tagged `tf-81`. Run `34480452541` uploaded CPV **81** and `ASSIGN Internal 81 204`. Dist `TPTQ6GKWZ7`. ASC `b4d4d85f-7f42-4338-a38c-58e0c3ae4591` **VALID**. PATCH 200. Keep Map mounted, classify hold water, named marks survive pack re-read. Leave 80 until TestFlight shows 81 Ready. Watch omitted. Internal only. Do not re-upload 54–80.

## HUD 82 Internal (Crisis GO)

HUD tree `323aa7f` tagged `tf-82`. Run `34491969097` uploaded CPV **82** and `ASSIGN Internal 82 204`. Dist `8872S8A8NY`. ASC `f5262825-9fc6-44a3-90c5-4eade1cee20b` **VALID**. PATCH 200. Textless compass on the home screen and boot; overlay titles carry the mark; selected tab is the reticle; SOS silver ring; INSTRUMENTS is HUD. Leave 81 until TestFlight shows 82 Ready. Watch omitted. Internal only. Do not re-upload 54–81.

## HUD 83 Internal (Crisis GO)

HUD tree `3abaf58` tagged `tf-83`. Run `34502826993` uploaded CPV **83** and `ASSIGN Internal 83 204`. Dist `947QX8HSZ9`. ASC `9ef5ecfb-f11a-40a8-b2c9-27148f0a02ea` **VALID**. PATCH 200. Expedition condition rails; FIELD book + VISION still (pack-book name / UNKNOWN / NO VISION MODEL, never edible); COMMS HOLD PTT, real mic clip, LEAVE NET. Leave 82 until TestFlight shows 83 Ready. Watch omitted. Internal only. Do not re-upload 54–82.

## HUD 84 Internal (Crisis GO)

HUD tree `4ec42d4` tagged `tf-84`. Run `34536953430` uploaded CPV **84** and `ASSIGN Internal 84 204`. Dist `WK9VCHNP39`. ASC `e0ac0030-87e3-4952-b2a7-8336dffe99ef` **VALID**. PATCH 200. Unsigned compile [34536170362](https://github.com/crisiskhan/Blackout/actions/runs/34536170362) (generic device + simulator). No fake party body on YOU; highway refs and the walk line silver; water class labels off the canvas; hit/mark rows fade; LAYOUT can place SOS. Leave 83 until TestFlight shows 84 Ready. Watch omitted. Internal only. Do not re-upload 54–83.

## HUD 85 Internal (Crisis GO)

HUD tree `487f290` tagged `tf-85`. Run `34547063820` uploaded CPV **85** and `ASSIGN Internal 85 204`. Dist `JVJ9WKTV5A`. ASC `5cbd4db3-9642-4892-bef4-e8542a75afa9` **VALID**. PATCH 200. Unsigned compile [34546255034](https://github.com/crisiskhan/Blackout/actions/runs/34546255034) (generic device + simulator). Hold woodland/scrub/peak/hole opens plant, bite, animal, cave cards; a FIELD still of the same kind offers the same procedure. TX WEST sinkhole 31.694905, −106.441133. No wildlife GPS. Never edible. Leave 84 until TestFlight shows 85 Ready. Watch omitted. Internal only. Do not re-upload 54–84.

## HUD 86 Internal (Crisis GO)

HUD tree `0d0e0b3` tagged `tf-86`. Run [34550033439](https://github.com/crisiskhan/Blackout/actions/runs/34550033439) uploaded CPV **86** and `ASSIGN Internal 86 204`. Dist `6KKC39285N`. ASC `f1a73104-c56c-4171-ba94-c08b0853c716` **VALID**. PATCH 200. Unsigned compile [34549187846](https://github.com/crisiskhan/Blackout/actions/runs/34549187846) (generic device + simulator). Woodland `FIELD · PLANT` opens tree-use first; `BOOK` / `CARD n OF`; a named tree is tree cards only; a hole is `FIELD · CAVE` then `NEXT · COLD`. No wildlife GPS. Never edible. Leave 85 until TestFlight shows 86 Ready. Watch omitted. Internal only. Do not re-upload 54–85.

## HUD 87 Internal (Crisis GO)

HUD tree `2c91036` tagged `tf-87`. Run [34572153310](https://github.com/crisiskhan/Blackout/actions/runs/34572153310) uploaded CPV **87** and `ASSIGN Internal 87 204`. Dist `SJ7LCMGH3L`. ASC `6ab6b596-0c4f-4a60-89f6-6cc39dfad22b` **VALID**. PATCH 200. Unsigned compile [34571356306](https://github.com/crisiskhan/Blackout/actions/runs/34571356306) (generic device + simulator). A Texas peak is `FIELD · ANIMAL` then `NEXT · BITE` then `NEXT · COLD`. NM mammal SPEAK names the bite card. No wildlife GPS. Never edible. Leave 86 until TestFlight shows 87 Ready. Watch omitted. Internal only. Do not re-upload 54–86. Do not retag 85, 86, or 87.

## HUD 88 Internal (Crisis GO)

HUD tree `6295e73` tagged `tf-88`. Run [34629533409](https://github.com/crisiskhan/Blackout/actions/runs/34629533409) uploaded CPV **88** and `ASSIGN Internal 88 204`. Dist `4JYX5MF425`. ASC `a0a32168-94a7-422d-8204-31332e32eaa1` **VALID**. PATCH 200. Unsigned compile [34629197731](https://github.com/crisiskhan/Blackout/actions/runs/34629197731) on the tagged tree. Notable OSM extract: named nature-reserve relations, cave mouths, named trees; karst / wildlife preserve / Audubon / nature area / habitat preserve Holds. No wildlife GPS. Never edible. Leave 87 until TestFlight shows 88 Ready, then install `0.1.0 (88)`. Watch omitted. Internal only. Do not re-upload 54–87. Do not retag 85, 86, 87, or 88.

## HUD 89 Internal (Crisis GO)

HUD tree `c1388f3` tagged `tf-89`. Run [34648083111](https://github.com/crisiskhan/Blackout/actions/runs/34648083111) uploaded CPV **89** and `ASSIGN Internal 89 204`. Dist `NVH99KH233`. ASC `24e2c615-db24-4f09-b362-563cdafd1d4c` **VALID**. PATCH 200. Unsigned compile generic iOS green on [34647718165](https://github.com/crisiskhan/Blackout/actions/runs/34647718165); simulator still in flight. FIELD SEARCH + from-nothing book. Botanic Holds: Lush n Lean Garden, Orchard Garden, Harvey Cornell Rose Park. No wildlife GPS. Never edible. Leave 88 until TestFlight shows 89 Ready, then install `0.1.0 (89)`. Watch omitted. Internal only. Do not re-upload 54–88. Do not retag 85–89.

## HUD 90 Internal (Crisis GO)

HUD tree `3eace8e` tagged `tf-90`. Run [34698148948](https://github.com/crisiskhan/Blackout/actions/runs/34698148948) uploaded CPV **90** and `ASSIGN Internal 90 204`. Dist `WQLW3QMVCJ`. ASC `9c8557d6-3735-44bd-9027-cd1b3bb712ac` **VALID**. PATCH 200. Unsigned compile [34697829522](https://github.com/crisiskhan/Blackout/actions/runs/34697829522) (generic device + simulator). MapLibreMapTests **Executed 175 tests, with 0 failures** (104.749s). FACE on YOU and party, Bennett Mountain, heading fail-closed (tick dark until accuracy is real). No wildlife GPS. Never edible. Leave 89 until TestFlight shows 90 Ready, then install `0.1.0 (90)`. Watch omitted. Internal only. Do not re-upload 54–89. **91** is caught. Leave 90 until TestFlight shows 91 Ready, then install `0.1.0 (91)`. Do not retag 85–90.

## HUD 91 Internal (Crisis GO)

HUD tree `6572ba2` tagged `tf-91`. Run [34702742398](https://github.com/crisiskhan/Blackout/actions/runs/34702742398) uploaded CPV **91** and `ASSIGN Internal 91 204`. Dist `QC3528L4VU`. KEEP `45YLWHL6UP` untouched. ASC `4bc30e84-fb08-4c6e-aba8-163788614c73` **VALID**. PATCH 200. Unsigned compile [34701892788](https://github.com/crisiskhan/Blackout/actions/runs/34701892788) (generic device + simulator). MapLibreMapTests **Executed 175 tests, with 0 failures** (114.035s). Empty polyline / rest-of-app fail-closed, puck/crash, San Antonio Mountain. IPA 91 is `6572ba2`. Big Brushy Mountain (`587c984`) is a later SHA and is not on 91. No wildlife GPS. Never edible. Leave 90 until TestFlight shows 91 Ready, then install `0.1.0 (91)`. Watch omitted. Internal only. Do not re-upload 54–90. **92** is caught. Leave 91 until TestFlight shows 92 Ready, then install `0.1.0 (92)`. Do not retag 85–91.

## HUD 92 Internal (Crisis GO)

HUD tree `4764bc5` tagged `tf-92`. Run [34712071568](https://github.com/crisiskhan/Blackout/actions/runs/34712071568) uploaded CPV **92** and `ASSIGN Internal 92 204`. Dist `635A8PMWLS`. KEEP `45YLWHL6UP` untouched. ASC `1faed6d4-3d3e-4a92-839e-ba3f023e6bed` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Crash-safe MAP SEARCH (rank off the HUD) + Western Honey Mesquite. IPA 92 is `4764bc5`. Castner / El Cerro / Galisteo (`4865b3d`) and later open-reserve Holds are later SHAs and are not on 92. No wildlife GPS. Never edible. Leave 91 until TestFlight shows 92 Ready, then install `0.1.0 (92)`. Watch omitted. Internal only. Do not re-upload 54–91. **93** is caught. Leave 92 until TestFlight shows 93 Ready, then install `0.1.0 (93)`. Do not retag 85–92.

## HUD 93 Internal (Crisis GO)

HUD tree `85e10d0` tagged `tf-93`. Run [34714005365](https://github.com/crisiskhan/Blackout/actions/runs/34714005365) uploaded CPV **93** and `ASSIGN Internal 93 204`. Dist `T4LZRLF5GZ`. KEEP `45YLWHL6UP` untouched. ASC `4fee472b-bce2-4779-8433-ed317bfa8c19` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Unsigned compile [34713786716](https://github.com/crisiskhan/Blackout/actions/runs/34713786716) (generic device + simulator). MapLibreMapTests **Executed 175 tests, with 0 failures** (107.484s). Hold overlay PIP bbox-rejects far sheets so a White Sands / Petroglyph press cannot stall the HUD. PTT clip refuses a 0-channel buffer. IPA 93 is `85e10d0` (Castner / Trackways / White Sands / Ojito / Tent Rocks / Petroglyph / Placitas / Cerrillos and the Hold PIP fix). IPA 92 (`4764bc5`) is not this tree. No wildlife GPS. Never edible. Leave 92 until TestFlight shows 93 Ready, then install `0.1.0 (93)`. Watch omitted. Internal only. Do not re-upload 54–92. Do not retag 85–93.

## HUD 99 Internal (historical)

HUD tree `adfb764` tagged `tf-99`. Run [34745805359](https://github.com/crisiskhan/Blackout/actions/runs/34745805359) uploaded CPV **99** and `ASSIGN Internal 99 204`. Dist `66KB44VS8G`. KEEP `45YLWHL6UP` untouched. ASC `960c7bd3-fe7e-47a2-a5dc-55c53ae9db55` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Unsigned compile [34745787525](https://github.com/crisiskhan/Blackout/actions/runs/34745787525): Blackout generic iOS device green; simulator leftover asserts. IPA 99 is live instruments, SPEAK street vertices, unique leftover Holds. Watch omitted. Internal only. Do not re-upload 54–98. Do not retag 85–99. Tree CPV stays 1. Tag `tf-114` (`0eb6c50`) archive failed: HUD keyboard mixed `Color` and `LinearGradient` in one stroke ternary. Scoring tip is tag `tf-117` / TestFlight `0.1.0 (110)`. Leave 109 until TestFlight shows 110 Ready, then install `0.1.0 (110)`. Do not install `0.1.0 (99)` unless scoring that old binary.

## HUD 100 Internal (no unique IPA)

HUD tree `cfd2668` tagged `tf-100`. Run [34745993995](https://github.com/crisiskhan/Blackout/actions/runs/34745993995) archived with Dist `F5Z894XRY6`. KEEP `45YLWHL6UP` untouched. Unsigned Blackout generic iOS device green on [34745989075](https://github.com/crisiskhan/Blackout/actions/runs/34745989075); simulator leftover asserts. Next CPV raced `tf-99` and minted **99**. altool `-19232` The bundle version must be higher than the previously uploaded version: ‘99’. No unique CPV 100 from this tag. Assign reused ASC `960c7bd3-fe7e-47a2-a5dc-55c53ae9db55` **VALID** and `ASSIGN Internal 99 204`. COORDINATES word off the MAP box is in later SHAs. TestFlight `0.1.0 (100)` is tag `tf-101` (`ce4f6bc`, CONDITION BLACK SOS), not this tree. Do not retag 85–99. Tree CPV stays 1. Scoring tip is tag `tf-117` / TestFlight `0.1.0 (110)`. Leave 109 until TestFlight shows 110 Ready, then install `0.1.0 (110)`.

## HUD 115 Internal (historical)

HUD tree `9c33aa8` tagged `tf-115`. Run [34752403614](https://github.com/crisiskhan/Blackout/actions/runs/34752403614) uploaded CPV **108** and `ASSIGN Internal 108 204`. Dist `8CWLXNF8C7`. KEEP `45YLWHL6UP` untouched. ASC `0315a666-fda5-4fed-924e-fdd1f47cdc2b` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Tag 115 minted 108 because tags 101–109 already used CPV 100–107. Tag `tf-109` is TestFlight `0.1.0 (107)` — not CPV 109. tf-110 through tf-114 failed archive (HUD keyboard mixed `Color` and `LinearGradient` through tf-114). IPA 108 is HUD keyboard live stroke accent / idle metal, Expedition NAME + TIME + SET, presets 30 MIN / 1 HR / 2 HRS. Overlay Holds after `9c33aa8` are later SHAs and are not on this IPA. Watch omitted. Internal only. Do not re-upload 54–107. Do not retag 85–115. Tree CPV stays 1. Scoring tip is tag `tf-117` / TestFlight `0.1.0 (110)`. Leave 109 until TestFlight shows 110 Ready, then install `0.1.0 (110)`.

## HUD 116 Internal (historical)

HUD tree `93a1b5d` tagged `tf-116`. Run [34759178121](https://github.com/crisiskhan/Blackout/actions/runs/34759178121) uploaded CPV **109** and `ASSIGN Internal 109 204`. Dist `2UCJZVQLL3`. KEEP `45YLWHL6UP` untouched. ASC `929c7c0e-0be8-4676-bc73-030f697ee76b` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Tag 116 minted 109 because max ASC was 108. This CPV **109** is tag `tf-116`, not tag `tf-109` (that older tag is TestFlight `0.1.0 (107)`). IPA 109 is HUD keyboard + Expedition presets plus overlay Holds after `9c33aa8`: Sandia Wash Western Honey Mesquite; Onate / Block / Contrabando / Cerros El Sabinoso; unique Valles Caldera and Sevilleta named peaks. Watch omitted. Internal only. Do not re-upload 54–108. Do not retag 85–116. Tree CPV stays 1. Scoring tip is tag `tf-117` / TestFlight `0.1.0 (110)`. Leave 109 until TestFlight shows 110 Ready, then install `0.1.0 (110)`. Do not wait for a build numbered 116.

## HUD 117 Internal (Crisis GO)

HUD tree `7fbaf83` tagged `tf-117`. Run [34763432123](https://github.com/crisiskhan/Blackout/actions/runs/34763432123) uploaded CPV **110** and `ASSIGN Internal 110 204`. Dist `DNYUTN8JX3`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `2UCJZVQLL3` revoked (not KEEP). ASC `f598d2c6-808b-45e5-be64-7283349b0ca0` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Tag 117 minted 110 because max ASC was 109. This CPV **110** is tag `tf-117`, not a build numbered 117. IPA 110 is default INVENTORY Water at 0, no WATER condition rail (THIRST covers hydration), no COMMS THREAD, CALL and MESSAGE on YOU and party, Expedition INVENTORY (not KIT), no APPLY RED BAND. Overlay Holds from IPA 109 are in this tree. IPA 110 does not include the incoming HUD line or the full-page poster overlay. Watch omitted. Internal only. Do not re-upload 54–109. Do not retag 85–117. Tree CPV stays 1. Leave 109 until TestFlight shows 110 Ready, then install `0.1.0 (110)`. Do not wait for a build numbered 117.

## HUD 118 Internal (Crisis GO)

HUD tree `360d1b9` tagged `tf-118`. Run [34766334917](https://github.com/crisiskhan/Blackout/actions/runs/34766334917) uploaded CPV **111** and `ASSIGN Internal 111 204`. Dist `MW9WJ342MW`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `DNYUTN8JX3` revoked (not KEEP). ASC `b228beba-04cf-4073-8866-a67d6a117f52` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Tag 118 minted 111 because max ASC was 110. This CPV **111** is tag `tf-118`, not a build numbered 118. IPA 111 is IPA 110 plus the incoming CALL/MESSAGE HUD glass line. It still crops the boot mark in a 0.78 well. The full-page poster overlay (black stays) is tag `tf-119` / TestFlight `0.1.0 (112)`, not this IPA. Watch omitted. Internal only. Do not re-upload 54–110. Do not retag 85–118. Tree CPV stays 1. Leave **110** until TestFlight shows **111** Ready, then install `0.1.0 (111)`. Do not wait for a build numbered 118.

## HUD 119 Internal (Crisis GO)

HUD tree `0a1b83b` tagged `tf-119`. Run [34767753179](https://github.com/crisiskhan/Blackout/actions/runs/34767753179) uploaded CPV **112** and `ASSIGN Internal 112 204`. Dist `2NV89KF3X3`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `MW9WJ342MW` revoked (not KEEP). ASC `a48a552d-b775-4e18-8165-aa8fd89a6f5e` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Tag 119 minted 112 because max ASC was 111. This CPV **112** is tag `tf-119`, not a build numbered 119. Unsigned compile [34767756348](https://github.com/crisiskhan/Blackout/actions/runs/34767756348): Blackout generic iOS device green; simulator leftover asserts. IPA 112 is IPA 111 plus the full-page BLACKOUT poster overlay: black stays on the outside, no 0.78 crop well, no knockout alpha. Incoming CALL/MESSAGE glass line is in this tree. Watch omitted. Internal only. Do not re-upload 54–111. Do not retag 85–119. Tree CPV stays 1. Leave **111** until TestFlight shows **112** Ready, then install `0.1.0 (112)`. Do not wait for a build numbered 119.

## HUD 120 Internal (no unique IPA)

HUD tree `1b880e8` tagged `tf-120`. Run [34788757841](https://github.com/crisiskhan/Blackout/actions/runs/34788757841) archived with Dist `W98723J9XB`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `2NV89KF3X3` revoked (not KEEP). `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. CPV **113** in the archive product. IPA inspect then failed closed: `llama.framework` `CFBundleIdentifier=org.ggml.llama` was required to be `com.maplibre.mapbox`. No altool. No unique IPA. Do not retag 85–120. Tree CPV stays 1. Leave **112** until a later tag uploads. CPV **113** is tag `tf-121`.

## HUD 121 Internal (Crisis GO)

HUD tree `d4a6293` tagged `tf-121`. Run [34790219287](https://github.com/crisiskhan/Blackout/actions/runs/34790219287) uploaded CPV **113** and `ASSIGN Internal 113 204`. Dist `A27MVFW864`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `W98723J9XB` revoked (not KEEP). ASC `10c8ace3-13ce-455d-b4fd-271887f2b212` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. Tag 121 minted 113 because max ASC was 112. This CPV **113** is tag `tf-121`, not a build numbered 121. `exportArchive OK`. IPA inspect OK (no FMWK rewrite): vendor `org.ggml.llama` on `llama.framework`, `com.maplibre.mapbox` on MapLibre. altool `UPLOAD SUCCEEDED with no errors`. IPA 113 is FIELD SEARCH live ASK, open-card layout, FieldAsk device compile, MeshDTN leftover, and the llama vendor-BID inspect/re-sign fix. Watch omitted. Internal only. Do not re-upload 54–112. Do not retag 85–121. Tree CPV stays 1. Leave **112** until TestFlight shows **113** Ready, then install `0.1.0 (113)`. Do not wait for a build numbered 120 or 121.

## HUD 176 Internal (no unique IPA)

CCTV tree `a227799` tagged `tf-176`. Run [35230916810](https://github.com/crisiskhan/Blackout/actions/runs/35230916810) archived CPV **149**. Dist `D444F3KQB7`. KEEP `45YLWHL6UP` untouched. IPA **4207460012** bytes. altool uploaded, then ASC `1463e5f8-6028-4620-8bb8-8a9b5b8593e8` **INVALID**. Same 4 GB ceiling as tf-174 CPV 148. No Internal assign. Unsigned compile on `a227799` was green ([35229679356](https://github.com/crisiskhan/Blackout/actions/runs/35229679356) generic iOS). Do not retag 150–176. Tree CPV stays 1. CPV **150** is tag `tf-177`.

## HUD 177 Internal (Crisis GO)

CCTV tree `545b7b2` tagged `tf-177`. Run [35236202236](https://github.com/crisiskhan/Blackout/actions/runs/35236202236) uploaded CPV **150** and `ASSIGN Internal 150 204`. Dist `5684ZXQZ73`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `D444F3KQB7` revoked (not KEEP). ASC `76e89783-1568-40f0-95f9-4dd9fa858190` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469009137** bytes. Unsigned compile [35234754941](https://github.com/crisiskhan/Blackout/actions/runs/35234754941) Blackout generic iOS device green (job 105247679730). Simulator MapLibre abort is pre-existing and ignored. IPA 150 is packed CCTV split discs + SNAP stills; NM photo stays in git and off the phone so the IPA fits. Tag 177 minted 150 because max ASC was 149. This CPV **150** is tag `tf-177`, not a build numbered 177. Watch omitted. Internal only. Do not re-upload 54–149. Do not retag 150–177. Tree CPV stays 1. Historical — scoring tip is tag `tf-178` / TestFlight `0.1.0 (151)`. Leave **150** until TestFlight shows **151** Ready, then install `0.1.0 (151)`.

## HUD 178 Internal (Crisis GO)

CCTV tree `304a914` tagged `tf-178`. Run [35275834080](https://github.com/crisiskhan/Blackout/actions/runs/35275834080) uploaded CPV **151** and `ASSIGN Internal 151 204`. Dist `3FRJL249S4`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `5684ZXQZ73` revoked (not KEEP). ASC `6e68c343-a27c-4391-b785-1991017d7bae` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469008911** bytes. Unsigned compile [35275014597](https://github.com/crisiskhan/Blackout/actions/runs/35275014597) Blackout generic iOS device green (job 105383398574). Simulator MapLibre abort is pre-existing and ignored. IPA 151 is IPA 150 plus phone-style strip of missing NAIP and PackStyle resolver v14, so a 147 cache cannot keep `file://` URLs to aerial archives the IPA no longer ships. Switching to NM must not crash. Tag 178 minted 151 because max ASC was 150. This CPV **151** is tag `tf-178`, not a build numbered 178. Watch omitted. Internal only. Do not re-upload 54–150. Do not retag 150–178. Tree CPV stays 1. Historical — scoring tip is tag `tf-179` / TestFlight `0.1.0 (152)`. Leave **151** until TestFlight shows **152** Ready, then install `0.1.0 (152)`.

## HUD 179 Internal (Crisis GO)

FIELD tree `ec3b586` tagged `tf-179`. Run [35290763245](https://github.com/crisiskhan/Blackout/actions/runs/35290763245) uploaded CPV **152** and `ASSIGN Internal 152 204`. Dist `GAK7G43DQA`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `3FRJL249S4` revoked (not KEEP). ASC `22d8dbc9-f996-4c62-8145-1236549cf7e3` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469219881** bytes. Unsigned compile [35290748881](https://github.com/crisiskhan/Blackout/actions/runs/35290748881) Blackout generic iOS device green. Simulator FieldAsk `testInfantChokeIsBackAndChestNotBelly` and FieldCorpus `can't breathe` → `med-airway` failed on that SHA; both are fixed on mesh-near `1cc44f5`. IPA 152 is the field-medic tree: first move now, CAUSE chips in likelihood order, BACK. Tag 179 minted 152 because max ASC was 151. This CPV **152** is tag `tf-179`, not a build numbered 179. Watch omitted. Internal only. Do not re-upload 54–151. Do not retag 150–179. Tree CPV stays 1. Historical — scoring tip is tag `tf-181` / TestFlight `0.1.0 (154)`. Leave **153** until TestFlight shows **154** Ready, then install `0.1.0 (154)`.

## HUD 180 Internal (Crisis GO)

Mesh-near tree `1cc44f5` tagged `tf-180`. Run [35293346292](https://github.com/crisiskhan/Blackout/actions/runs/35293346292) uploaded CPV **153** and `ASSIGN Internal 153 204`. Dist `9FQZG9MZ95`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `GAK7G43DQA` revoked (not KEEP). ASC `ed3eda8a-d8b8-4e2a-8f54-154fdf3a0481` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469309555** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 153`, then VALID. Unsigned compile [35293264919](https://github.com/crisiskhan/Blackout/actions/runs/35293264919) on `1cc44f5`: Blackout generic iOS device green (job 105440460879). Simulator FieldAsk 10 tests 0 failures (job 105440460663) — infant first move names the belly. FieldCorpus 15 tests 0 failures — `can't breathe` is the live look. IPA 153 is NEAR dots + hop carry + infant belly + `can't breathe` look. Tag 180 minted 153 because max ASC was 152. This CPV **153** is tag `tf-180`, not a build numbered 180. Watch omitted. Internal only. Do not re-upload 54–152. Do not retag 150–180. Tree CPV stays 1. Historical — scoring tip is tag `tf-181` / TestFlight `0.1.0 (154)`. Leave **153** until TestFlight shows **154** Ready, then install `0.1.0 (154)`. Do not wait for a build numbered 180.

## HUD 181 Internal (Crisis GO)

Photo-desk tree `32a0ad2` tagged `tf-181`. Run [35299262226](https://github.com/crisiskhan/Blackout/actions/runs/35299262226) uploaded CPV **154** and `ASSIGN Internal 154 204`. Dist `2SK8A5QCM9`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `9FQZG9MZ95` revoked (not KEEP). ASC `bea37b7d-ffd7-451a-a7d1-95d999542c74` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469334742** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 154`, then VALID. Unsigned compile [35299261194](https://github.com/crisiskhan/Blackout/actions/runs/35299261194) on `32a0ad2`: Blackout generic iOS device green (job 105458226957). Simulator green (job 105458226745). IPA 154 is IPA 153 plus no grey house masses on the photo, tree/water/worked-ground sheets hidden on packed NAIP, OSM credit above the pack footer, pins on top of WALK/tails, and `NEAR · LIVE` held across a second prune. Tag 181 minted 154 because max ASC was 153. This CPV **154** is tag `tf-181`, not a build numbered 181. Watch omitted. Internal only. Do not re-upload 54–153. Do not retag 150–181. Tree CPV stays 1. Historical — scoring tip is tag `tf-182` / TestFlight `0.1.0 (155)`. Leave **154** until TestFlight shows **155** Ready, then install `0.1.0 (155)`.

## HUD 182 Internal (Crisis GO)

Photo-desk tree `b32b344` tagged `tf-182`. Run [35303551868](https://github.com/crisiskhan/Blackout/actions/runs/35303551868) uploaded CPV **155** and `ASSIGN Internal 155 204`. Dist `5RHV6UMX77`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `2SK8A5QCM9` revoked (not KEEP). ASC `d5e72674-de74-4acb-8633-1c806739537f` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469335131** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 155`, then VALID. Unsigned compile [35303542762](https://github.com/crisiskhan/Blackout/actions/runs/35303542762) on `b32b344`: Blackout generic iOS device green (job 105470987266). Simulator green (job 105470987366). IPA 155 is IPA 154 plus dest pins above the first WALK void halo, ASK cancel on clear SEARCH / pack switch, NEAR hold demoted to LAST HEARD when radios go quiet, and VISION generation-gated. Tag 182 minted 155 because max ASC was 154. This CPV **155** is tag `tf-182`, not a build numbered 182. Watch omitted. Internal only. Do not re-upload 54–154. Do not retag 150–182. Tree CPV stays 1. Historical — scoring tip is tag `tf-183` / TestFlight `0.1.0 (156)`. Leave **155** until TestFlight shows **156** Ready, then install `0.1.0 (156)`. Do not wait for a build numbered 182.

## HUD 183 Internal (Crisis GO)

Mesh-scan tree `e3ef714` tagged `tf-183`. Run [35340721914](https://github.com/crisiskhan/Blackout/actions/runs/35340721914) uploaded CPV **156** and `ASSIGN Internal 156 204`. Dist `6WGC4GGVHS`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `5RHV6UMX77` revoked (not KEEP). ASC `806c9402-19dc-4b67-8dbf-25a22fdf82f8` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469344272** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 156`, then VALID. Unsigned compile [35338816314](https://github.com/crisiskhan/Blackout/actions/runs/35338816314) on `e3ef714`: Blackout generic iOS device green (job 105579784732). Simulator green (job 105579784519). IPA 156 is IPA 155 plus SCAN/JOIN place no-fix hears as a reach ring around YOU; LISTEN stays chrome-only; hold `WALK — NO PLACE`; `SCAN — NO FIX` without GNSS. Tag 183 minted 156 because max ASC was 155. This CPV **156** is tag `tf-183`, not a build numbered 183. Watch omitted. Internal only. Do not re-upload 54–155. Do not retag 150–183. Tree CPV stays 1. Historical — scoring tip is tag `tf-184` / TestFlight `0.1.0 (157)`. Leave **156** until TestFlight shows **157** Ready, then install `0.1.0 (157)`. Do not wait for a build numbered 183.

## HUD 184 Internal (Crisis GO)

Condition-rail tree `66d6e0c` tagged `tf-184`. Run [35357713203](https://github.com/crisiskhan/Blackout/actions/runs/35357713203) uploaded CPV **157** and `ASSIGN Internal 157 204`. Dist `536BH2LGA9`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `6WGC4GGVHS` revoked (not KEEP). ASC `f0dfe7aa-8d7e-430c-8198-f3ef50ebee77` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469430803** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 157`, then VALID. Unsigned compile [35357107738](https://github.com/crisiskhan/Blackout/actions/runs/35357107738) on `66d6e0c`: Blackout generic iOS device green (job 105639047760). Simulator MeshDTNTests `txPower` argument order failed (job 105639048223) — test-only, not in the IPA. TripBrief exit=0. IPA 157 is IPA 156 plus sealed party bodies, DIARY under ROSTER members (HERE/SILENT, TODAY/LOG newest-first, PAPER), and glass capsule CONDITION rails that fill to the live band (BLACK + SOS). Tag 184 minted 157 because max ASC was 156. This CPV **157** is tag `tf-184`, not a build numbered 184. Watch omitted. Internal only. Do not re-upload 54–156. Do not retag 150–184. Tree CPV stays 1. Historical — scoring tip is tag `tf-185` / TestFlight `0.1.0 (158)`. Leave **157** until TestFlight shows **158** Ready, then install `0.1.0 (158)`. Do not wait for a build numbered 184.

## HUD 185 Internal (Crisis GO)

Mesh-hear tree `b41b8c7` tagged `tf-185`. Run [35372832850](https://github.com/crisiskhan/Blackout/actions/runs/35372832850) uploaded CPV **158** and `ASSIGN Internal 158 204`. Dist `P3SZNNKP7D`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `536BH2LGA9` revoked (not KEEP). ASC `c2431345-9098-463d-b106-34d82fee8af8` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3469430781** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 158`, then VALID. Unsigned compile [35372827289](https://github.com/crisiskhan/Blackout/actions/runs/35372827289) on `b41b8c7`: Blackout generic iOS device green (job 105690566762). Simulator green (job 105690566587). IPA 158 is IPA 157 plus the MeshHear test labels (`txPower` before `connectable`) so the hold-glass suite compiles on Swift 6. Same capsules and diary as 157. Tag 185 minted 158 because max ASC was 157. This CPV **158** is tag `tf-185`, not a build numbered 185. Watch omitted. Internal only. Do not re-upload 54–157. Do not retag 150–185. Tree CPV stays 1. Historical — scoring tip is tag `tf-186` / TestFlight `0.1.0 (159)`. Leave **158** until TestFlight shows **159** Ready, then install `0.1.0 (159)`. Do not wait for a build numbered 185.

## HUD 186 Internal (Crisis GO)

Photo-seam tree `168c7f6` tagged `tf-186`. Run [35448323976](https://github.com/crisiskhan/Blackout/actions/runs/35448323976) uploaded CPV **159** and `ASSIGN Internal 159 204`. Dist `9Z74QKR88Y`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `P3SZNNKP7D` revoked (not KEEP). ASC `f81c6e01-0072-47b3-aa9e-04f6747e7ff9` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3493500636** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 159`, then VALID. Unsigned compile [35418790150](https://github.com/crisiskhan/Blackout/actions/runs/35418790150) on `168c7f6`: Blackout generic iOS device green (job 105832430794). Simulator green (job 105832430945). IPA 159 is IPA 158 plus STATES (TX+NM on one flat glass, WALK/DRIVE `OFF GRAPH`, hold LAND) and the TX WEST walk-desk feather (`aerial-seam.pmtiles` last-wins, resolver v16). Doniphan / Artcraft is one photo, not a NAIP quilt. Tag 186 minted 159 because max ASC was 158. This CPV **159** is tag `tf-186`, not a build numbered 186. Watch omitted. Internal only. Do not re-upload 54–158. Do not retag 150–186. Tree CPV stays 1. Historical — scoring tip is tag `tf-187` / TestFlight `0.1.0 (160)`. Leave **159** until TestFlight shows **160** Ready, then install `0.1.0 (160)`. Do not wait for a build numbered 186.

## HUD 187 Internal (Crisis GO)

Drop-khan-eye tree `06d98b6` tagged `tf-187`. Run [35463190384](https://github.com/crisiskhan/Blackout/actions/runs/35463190384) uploaded CPV **160** and `ASSIGN Internal 160 204`. Dist `D48KPGX9LT`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `9Z74QKR88Y` revoked (not KEEP). ASC `33b38bb8-109c-4d4b-b312-1408abdc3db2` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3493435231** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 160`, then VALID. Unsigned compile [35457350562](https://github.com/crisiskhan/Blackout/actions/runs/35457350562) on `06d98b6`: Blackout generic iOS device green (job 105934883734). Simulator MapLibreMapTests **Executed 178 tests, with 0 failures** (job 105934883644). IPA 160 is IPA 159 plus KHAN EYE gone: overlay INSTRUMENTS / LOCK-ON / UPDATE, walking pinch ceiling 17.5, MARK kinds on the INSTRUMENTS MAP plate, no extract camera. Tag 187 minted 160 because max ASC was 159. This CPV **160** is tag `tf-187`, not a build numbered 187. Watch omitted. Internal only. Do not re-upload 54–159. Do not retag 150–187. Tree CPV stays 1. Historical — scoring tip is tag `tf-188` / TestFlight `0.1.0 (161)`. Leave **160** until TestFlight shows **161** Ready, then install `0.1.0 (161)`. Do not wait for a build numbered 187.

## HUD 188 Internal (Crisis GO)

Dead-glass tree `aef6a73` tagged `tf-188`. Run [35582125243](https://github.com/crisiskhan/Blackout/actions/runs/35582125243) uploaded CPV **161** and `ASSIGN Internal 161 204`. Dist `SBV25QJ9C6`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `D48KPGX9LT` revoked (not KEEP). ASC `92522e5e-6caf-48ae-afd5-88bf30032123` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3493501831** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 161`, then VALID. Unsigned compile [35580145018](https://github.com/crisiskhan/Blackout/actions/runs/35580145018) on `aef6a73`: Blackout generic iOS device green (job 106270881893). Simulator green (job 106270882221). IPA 161 is IPA 160 plus the 1b22 stack after the EYE cut: DELETE / NAME / COLOR on planted marks, hold-card scroll under the 50% cap, ten field COMMS chips, FIELD session on AppRuntime, and dead-glass cuts (POWER is POCKET only, GNSS PUCK off the glass, USB-C `NO CABLE`, CLIP PLAY, EXPORT PAPER share sheet, no JOIN NAV, no VISION lookalikes). Tag 188 minted 161 because max ASC was 160. This CPV **161** is tag `tf-188`, not a build numbered 188. Watch omitted. Internal only. Do not re-upload 54–160. Do not retag 150–188. Tree CPV stays 1. Historical — scoring tip is tag `tf-189` / TestFlight `0.1.0 (162)`. Leave **161** until TestFlight shows **162** Ready, then install `0.1.0 (162)`. Do not wait for a build numbered 188.

## HUD 189 Internal (Crisis GO)

Map-crash tree `e41bcb9` tagged `tf-189`. Run [35597277741](https://github.com/crisiskhan/Blackout/actions/runs/35597277741) uploaded CPV **162** and `ASSIGN Internal 162 204`. Dist `S986PFDJ7G`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `SBV25QJ9C6` revoked (not KEEP). ASC `00b3bb36-31e4-4670-b281-055687cde986` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3493485365** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 162`, then VALID. Unsigned compile [35596542300](https://github.com/crisiskhan/Blackout/actions/runs/35596542300) on `e41bcb9`: Blackout generic iOS device green (job 106322552022). Simulator green (job 106322551811). IPA 162 is IPA 161 plus wired HOLD PTT (no USB-C toggle), POCKET on BODY, PAPER and FACE off the glass, CONDITION ORANGE on three mid-yellow rails, and PACK-switch style-swap / finite-geometry crash guards. Tag 189 minted 162 because max ASC was 161. This CPV **162** is tag `tf-189`, not a build numbered 189. Watch omitted. Internal only. Do not re-upload 54–161. Do not retag 150–189. Tree CPV stays 1. Historical — scoring tip is tag `tf-190` / TestFlight `0.1.0 (163)`. Leave **162** until TestFlight shows **163** Ready, then install `0.1.0 (163)`. Do not wait for a build numbered 189.

## HUD 190 Internal (Crisis GO)

Desk-hold tree `b28c349` tagged `tf-190`. Run [35627814042](https://github.com/crisiskhan/Blackout/actions/runs/35627814042) uploaded CPV **163** and `ASSIGN Internal 163 204`. Dist `KJP9DGH63Z`. KEEP `45YLWHL6UP` untouched. Stale runner Dist `S986PFDJ7G` revoked (not KEEP). ASC `463fd5c8-8a5a-4d39-ab91-00ce589fdb72` **VALID**. PATCH 200. `usesNonExemptEncryption: null`. IPA **3493523793** bytes. macos-15 / Xcode 26.3. `HAS_LOCAL_DIST_KEY=1`. `exportArchive OK`. altool `UPLOAD SUCCEEDED with no errors`. `WAIT no build 163`, then VALID. Unsigned compile [35625742847](https://github.com/crisiskhan/Blackout/actions/runs/35625742847) on `b28c349`: Blackout generic iOS device green (job 106419559875). Simulator green (job 106419560146). IPA 163 is IPA 162 plus desk-hold: last-known YOU, DEST, CONDITION, kit, timers, FIELD card, POCKET / MAG/TRUE / LEFT HAND / ES/EN survive kill; a live WALK or DRIVE line replans silently; hold FIELD speaks the first move; NaN YOU and eye lines stay off MapLibre. Tag 190 minted 163 because max ASC was 162. This CPV **163** is tag `tf-190`, not a build numbered 190. Watch omitted. Internal only. Do not re-upload 54–162. Do not retag 150–190. Tree CPV stays 1. Leave **162** until TestFlight shows **163** Ready, then install `0.1.0 (163)`. Do not wait for a build numbered 190.

